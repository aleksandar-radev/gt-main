// Placeholder service file for future business logic.
// API calls are performed directly in stores for now.

export interface GamePayload {
    name: string;
    title: string;
    description: string;
    type: string;
    logoUrl?: string;
    bigLogoUrl?: string;
    url: string;
    isFeatured: boolean;
    status: string;
}

// TODO: add business logic helper functions here when needed
